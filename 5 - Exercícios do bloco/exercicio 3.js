const valorA = 10;

const valorB = 12;

const valorC = 20;

if (valorA > valorB && valorA > valorC) 
    console.log (`valor ${valorA} é o maior de todos`);

if (valorB > valorA && valorB > valorC)
    console.log(`valor ${valorB} é o maior de todos`);

if (valorC > valorA && valorC > valorB)
    console.log(`valor ${valorC} é o maior de todos`);